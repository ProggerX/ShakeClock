# Shake Clock

> **WARNING: THIS IS VIBECODED SHIT.**
>
> This entire project was generated by an AI ("vibe coded"). It was not designed,
> written, reviewed, or tested by a human engineer. It may be wrong, insecure,
> inefficient, ugly, or outright broken. Do not trust it. Read every line before
> you run it. You have been warned.

[English](#english) | [Русский](#русский)

---

## English

An Always-On Glyph toy for the **Nothing Phone (4a) Pro** (`Glyph.DEVICE_25111p`,
13x13 Glyph Matrix, AOD toys only). The matrix stays dark to spare the LEDs:
shake the phone and it lights up for 10 seconds, then goes dark again.
Shake again while it is lit to cycle through the enabled sections.

### Features

- Shake-to-show display with a configurable threshold slider (8–60 m/s²).
- Digital or analogue clock face.
- Date section (`DD / MM` or `MM / DD`).
- Optional temperature section via [Open-Meteo](https://open-meteo.com/) with `°C` / `°F`.
- Optional **Glyph Museum** animation section: import frame JSON, tick which ones to cycle.
- Idle display modes: `Off` (dark until shaken), `Animation` (idle plays the animation), `Keep` (idle keeps the last section).
- 12-hour / 24-hour / system time format.
- "Nothing style" Material 3 UI with Monét (Material You) dynamic colors and an NDot header.
- Everything is configured in-app and stored in `SharedPreferences`.

### Requirements

- Nothing Phone (4a) Pro (nothing else is supported by the Glyph Matrix SDK path used here).
- Nothing OS with Glyph Matrix support and AOD toys (`Flip to Glyph`).
- Nix with flakes enabled (for the reproducible dev shell), or Android Studio with JDK 17.

### Project structure

```
ShakeClock/
├── app/
│   ├── build.gradle.kts            app module, release signing config
│   ├── proguard-rules.pro
│   ├── libs/glyph-matrix-sdk-2.0.aar
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/proggerx/shakeclock/
│       │   ├── ShakeClockApp.kt          dynamic color (Monét) application
│       │   ├── MainActivity.kt           Material 3 settings UI
│       │   ├── ShakeClockToyService.kt   AOD glyph toy (shake + rendering)
│       │   ├── GlyphAnimation.kt         Glyph Museum JSON parser
│       │   ├── AnimationStore.kt         imported animations + selection
│       │   ├── ToySettings.kt            SharedPreferences
│       │   └── WeatherRepository.kt      Open-Meteo temperature
│       └── res/ ...                      layouts, drawables, strings
├── dist/                          signed release APKs (gitignored)
├── flake.nix / flake.lock         pinned Nix dev shell
├── build.gradle.kts / settings.gradle.kts / gradle.properties
├── keystore.properties.example    copy to keystore.properties for signing
├── .gitignore
├── LICENSE                        GPL-3.0
└── README.md
```

### Build (Nix only)

The Nix dev shell pins nixpkgs, JDK 17, Gradle 8 and a minimal Android SDK
(platform 35 + build-tools 35.0.0). No Android Studio required.

Debug build:

```sh
nix develop -c gradle assembleDebug
# -> app/build/outputs/apk/debug/app-debug.apk
```

Release build:

```sh
nix develop -c gradle assembleRelease
# -> app/build/outputs/apk/release/app-release.apk
```

Release signing reads `keystore.properties` (copy `keystore.properties.example`
and fill it in). Both the keystore and `keystore.properties` are gitignored and
must never be committed. If `keystore.properties` is missing, the release build
falls back to the debug key. The APK to upload to a GitHub release lives at
`dist/ShakeClock-<version>.apk`.

### Install

```sh
nix develop -c adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### Enable the toy

There is no universal public entry point on the 4a Pro for the AOD toy list, so
open it manually:

**Settings → Glyph Interface → Flip to Glyph → Always-on Glyph Toy → Shake Clock**

Then shake the phone.

### Glyph Museum animations

The Glyph Museum app keeps its frames private (not debuggable, scoped storage),
so this toy can't read them directly. Instead, import exported frame data:

1. On [editor.glyphmuseum.com](https://editor.glyphmuseum.com) pick the **Phone 4a**
   profile and design an animation.
2. Export the **Data** tab as **JSON** (`{"v":4,"frames":[{"d":..,"p":[137 values]}]}`).
3. In Shake Clock, tap **Import JSON** and pick the file. Import as many as you like.
4. Tick the animations you want included in the single **Animation** section. Shake
   cycles: time → date → temperature (if on) → animation, and the animation section
   plays the ticked animations in sequence.

`Remove all` clears every imported animation.

### Warnings and disclaimers

- **Vibecoded, untested, unmaintained.** No tests exist. No warranty of any kind.
  No support. Use entirely at your own risk.
- **Your phone, your problem.** Flashing the Glyph Matrix, running a background
  service, and holding a continuous accelerometer listener are not free. Expect
  extra battery drain. Long-term effects on the LEDs are unknown.
- **Photosensitivity.** Flashing or blinking lights can trigger seizures in people
  with photosensitive epilepsy. Be careful.
- **Location and privacy.** The optional temperature section reads your last known
  approximate location and sends its coordinates to Open-Meteo over the network to
  fetch the current temperature. It caches the result on-device for 20 minutes.
  If you do not want this, leave the temperature option off.
- **Permissions.** The app requests `INTERNET`, `ACCESS_COARSE_LOCATION`, and the
  Nothing `com.nothing.ketchum.permission.ENABLE` permission.
- **No guarantees.** The author(s) are not responsible for any damage, data loss,
  bricked devices, dead LEDs, or anything else caused by this software.

### License

This project is free software licensed under the **GNU General Public License,
version 3 or later** (`GPL-3.0-or-later`). See [LICENSE](LICENSE).

Note: the bundled `glyph-matrix-sdk-2.0.aar` is the proprietary Nothing Glyph
Matrix SDK and is **not** covered by the GPL. It remains the property of Nothing
and is subject to Nothing's own terms. The GPL applies to this project's own
source code only.

---

## Русский

Always-On Glyph-игрушка для **Nothing Phone (4a) Pro** (`Glyph.DEVICE_25111p`,
матрица 13x13, только AOD-игрушки). Матрица по умолчанию выключена, чтобы беречь
светодиоды: встряхни телефон — она загорится на 10 секунд, потом снова погаснет.
Встряхни ещё раз, пока она горит, чтобы переключиться на следующую включённую
секцию.

### Возможности

- Показ по встряхиванию, с ползунком порога (8–60 м/с²).
- Цифровые или аналоговые часы.
- Секция даты (`ДД / ММ` или `ММ / ДД`).
- Необязательная секция температуры через [Open-Meteo](https://open-meteo.com/) с `°C` / `°F`.
- Необязательная секция анимаций **Glyph Museum**: импорт JSON с кадрами и выбор,
  какие из них крутить по кругу.
- Режимы простоя: `Off` (темно до встряхивания), `Animation` (в простое играет
  анимация), `Keep` (в простое остаётся последняя секция).
- Формат времени 12 / 24 часа / системный.
- UI в стиле Nothing на Material 3 с динамическими цветами Monét (Material You)
  и заголовком шрифтом NDot.
- Всё настраивается в приложении и хранится в `SharedPreferences`.

### Требования

- Nothing Phone (4a) Pro (ничего другого используемый путь Glyph Matrix SDK не поддерживает).
- Nothing OS с поддержкой Glyph Matrix и AOD-игрушек (`Flip to Glyph`).
- Nix с включёнными flakes (для воспроизводимого dev shell) либо Android Studio с JDK 17.

### Сборка (только Nix)

Dev shell в Nix фиксирует nixpkgs, JDK 17, Gradle 8 и минимальный Android SDK
(platform 35 + build-tools 35.0.0). Android Studio не нужна.

Debug-сборка:

```sh
nix develop -c gradle assembleDebug
# -> app/build/outputs/apk/debug/app-debug.apk
```

Release-сборка:

```sh
nix develop -c gradle assembleRelease
# -> app/build/outputs/apk/release/app-release.apk
```

Для подписи release используется `keystore.properties` (скопируй
`keystore.properties.example` и заполни). И keystore, и `keystore.properties`
в .gitignore — никогда их не коммить. Если `keystore.properties` нет, release
подписывается debug-ключом. APK для загрузки в GitHub release лежит в
`dist/ShakeClock-<версия>.apk`.

### Установка

```sh
nix develop -c adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### Включение игрушки

Универсального публичного входа к списку AOD-игрушек на 4a Pro нет, поэтому
открой вручную:

**Настройки → Glyph Interface → Flip to Glyph → Always-on Glyph Toy → Shake Clock**

После этого тряси телефон.

### Анимации Glyph Museum

Приложение Glyph Museum хранит кадры приватно (не debuggable, scoped storage),
поэтому игрушка не может прочитать их напрямую. Вместо этого импортируй
экспортированные данные:

1. На [editor.glyphmuseum.com](https://editor.glyphmuseum.com) выбери профиль
   **Phone 4a** и нарисуй анимацию.
2. Экспортируй вкладку **Data** как **JSON** (`{"v":4,"frames":[{"d":..,"p":[137 значений]}]}`).
3. В Shake Clock нажми **Import JSON** и выбери файл. Импортировать можно сколько
   угодно анимаций.
4. Отметь галочками те, что должны попасть в единую секцию **Animation**.
   Встряхивание переключает: время → дата → температура (если включена) →
   анимация, а секция анимации проигрывает отмеченные анимации по очереди.

`Remove all` удаляет все импортированные анимации.

### Предупреждения и отказ от ответственности

- **Вайбкод, без тестов, без поддержки.** Тестов нет. Гарантий нет. Поддержки
  нет. Используешь полностью на свой страх и риск.
- **Твой телефон — твои проблемы.** Мигающая матрица, фоновый сервис и постоянно
  включённый акселерометр не бесплатны. Жди повышенного расхода батареи.
  Долгосрочное влияние на светодиоды неизвестно.
- **Фоточувствительность.** Мигающий свет может вызвать приступ у людей с
  фоточувствительной эпилепсией. Будь осторожен.
- **Геолокация и приватность.** Необязательная секция температуры читает твою
  последнюю приблизительную геопозицию и отправляет координаты в Open-Meteo по
  сети, чтобы получить текущую температуру. Результат кэшируется на устройстве на
  20 минут. Если это не нужно — не включай температуру.
- **Разрешения.** Приложение запрашивает `INTERNET`, `ACCESS_COARSE_LOCATION` и
  разрешение Nothing `com.nothing.ketchum.permission.ENABLE`.
- **Никаких гарантий.** Автор(ы) не несут ответственности за любой ущерб, потерю
  данных, превращённые в кирпич устройства, мёртвые светодиоды или что-либо ещё,
  вызванное этим ПО.

### Лицензия

Этот проект — свободное ПО под лицензией **GNU General Public License, версия 3
или новее** (`GPL-3.0-or-later`). См. [LICENSE](LICENSE).

Примечание: входящий в комплект `glyph-matrix-sdk-2.0.aar` — это проприетарный
Nothing Glyph Matrix SDK, и он **не** покрывается GPL. Он остаётся собственностью
Nothing и подчиняется её условиям. GPL распространяется только на исходный код
этого проекта.
